import Link from "next/link";
import React from "react";

export const PokemonLink = () => {
  return (
      <div>
        <Link href={`pokemon/${id}`}>
          
        </Link>
      </div>
  );
}
